# 项目设计文件

## 1. 项目场景
**场景名称**：API Token 中转站（虚拟商品零售店）

**场景描述**：本项目模拟一个在线API Token中转站，提供多种AI模型（如OpenAI、Claude、Gemini等）的API Token充值和转发服务。用户可以购买不同面额的Token套餐，用于调用各种AI API接口。平台负责Token的库存管理、订单处理、用户余额管理以及员工权限控制。

**选择理由**：
- 符合课程要求的“经营场景”：虽然是虚拟商品，但完整包含商品、库存、订单、订单明细、会员、员工等核心实体。
- 业务流程清晰：用户浏览商品→下单→支付→获取Token→使用Token，与实体店铺的“选购→结账→取货”流程高度相似。
- 便于扩展：后续可加入会员积分、优惠券、补货（Token充值）等复杂业务。

## 2. 业务流程
以下是从顾客到完成交易的主要环节：

### 2.1 顾客（用户）流程
1. **注册/登录**：用户通过账号密码或第三方登录注册新账户，已有账户直接登录。
2. **浏览商品**：用户查看商品列表，包括不同AI模型的Token套餐（如“GPT-4 100K Token包”、“Claude 50K Token包”等），显示价格、库存状态。
3. **选择商品**：用户选择所需套餐，加入购物车或直接购买。
4. **下单**：用户确认订单信息（商品、数量、总价），生成订单。
5. **支付**：用户选择支付方式（模拟支付，如余额支付、支付宝、微信），完成支付。
6. **获取Token**：支付成功后，系统自动将Token充值到用户账户，用户可在“我的Token”页面查看余额。
7. **使用Token**：用户通过API接口调用时，系统扣除相应Token余额。
8. **查看记录**：用户可查看订单历史、Token使用记录、余额变动明细。

### 2.2 店员（客服/运营）流程
1. **处理咨询**：回复用户关于商品、订单、技术问题的咨询。
2. **订单管理**：查看订单状态，处理退款、异常订单。
3. **库存监控**：监控Token库存量，低于阈值时通知管理员补货。
4. **用户管理**：协助用户解决账户问题（如密码重置、余额查询）。

### 2.3 店长（管理员）流程
1. **商品管理**：上架新商品、下架旧商品、修改价格和描述。
2. **库存管理**：补充Token库存（从上游API提供商批量采购），设置库存预警。
3. **订单管理**：查看所有订单，处理争议订单，生成销售报表。
4. **员工管理**：添加员工账户，分配角色和权限。
5. **系统监控**：监控系统运行状态，查看API调用成功率、用户活跃度等。

### 2.4 交易完成闭环
```
用户浏览商品 → 加入购物车 → 提交订单 → 支付 → 系统扣减库存 → 生成Token余额 → 用户使用Token → 订单完成
```

## 3. 角色与职能清单
| 角色 | 职能描述 | 权限范围 |
|------|----------|----------|
| **店长（管理员）** | 全局管理，包括商品、库存、订单、员工、系统配置。拥有所有权限。 | 所有数据的增删改查；员工角色分配；系统设置。 |
| **店员（客服/运营）** | 处理用户咨询、订单异常、库存监控。 | 查看用户信息、订单信息；修改订单状态（如退款）；查看库存；不能修改商品价格、不能删除数据。 |
| **会员（用户）** | 购买商品、使用Token、查看个人记录。 | 查看商品；下单购买；查看自己的订单、余额、使用记录；修改个人信息。 |
| **游客（未注册用户）** | 浏览商品，不能下单。 | 仅查看商品列表和价格。 |

## 4. 数据边界清单
### 4.1 进库信息（存储到数据库）
| 数据类别 | 具体内容 | 理由 |
|----------|----------|------|
| **用户信息** | 用户ID、用户名、密码（加密）、手机号、邮箱、注册时间、状态（正常/冻结）。 | 核心业务实体，用于身份验证和订单关联。 |
| **商品信息** | 商品ID、名称、描述、价格、所属AI模型、创建时间、状态（上架/下架）。 | 核心业务实体，用于展示和订单生成。 |
| **库存信息** | 商品ID、当前库存量、库存阈值、最后更新时间。 | 控制销售，避免超卖；触发补货提醒。 |
| **订单信息** | 订单ID、用户ID、总金额、订单状态（待支付/已支付/已取消/已退款）、创建时间、支付时间。 | 记录交易，用于财务统计和售后。 |
| **订单明细** | 明细ID、订单ID、商品ID、数量、单价、小计。 | 记录订单具体商品，支持多商品订单。 |
| **员工信息** | 员工ID、姓名、角色（店长/店员）、账号、密码、入职时间、状态。 | 管理内部人员，控制权限。 |
| **Token余额** | 用户ID、商品ID（或AI模型）、剩余Token量、最后更新时间。 | 用户使用Token的核心数据。 |
| **Token使用记录** | 记录ID、用户ID、商品ID、使用量、使用时间、调用接口。 | 计费和审计依据。 |
| **角色权限表** | 角色ID、角色名称、权限描述。 | 控制不同角色的操作权限。 |

### 4.2 不进库信息（不存储）
| 数据类别 | 理由 |
|----------|------|
| **用户密码明文** | 安全考虑，只存储加密后的哈希值。 |
| **支付过程细节**（如第三方支付流水号） | 简化设计，仅记录支付状态和时间；支付流水号可由支付系统单独管理。 |
| **API调用的具体内容**（如用户发送的Prompt） | 隐私和存储成本考虑，仅记录Token使用量。 |
| **用户设备信息**（如IP、User-Agent） | 非核心业务数据，可由日志系统单独记录。 |
| **商品图片/文件** | 简化设计，仅用文字描述；实际项目中可存储URL。 |
| **物流信息** | 虚拟商品无需物流。 |

## 5. 核心实体关系（初步）
- 一个用户可以有多个订单。
- 一个订单可以包含多个订单明细（多商品）。
- 一个商品对应一个库存记录。
- 一个用户可以有多个Token余额记录（按AI模型区分）。
- 一个员工属于一个角色。

## 6. 后续阶段展望
- **第二阶段**：设计ER图，进行规范化，建立数据库表结构。
- **第三阶段**：实现存储过程、触发器、事务（如下单事务、补货事务）。
- **第四阶段**：接入自然语言转SQL，实现销量预测和智能补货。

## 7. 组内分工
- **阮依成**：负责整体场景设计、业务流程梳理、数据边界划定、文档整合。
- **陈诗翰**：负责角色与职能清单、权限设计、部分业务流程细化。
- **肖懿**：负责数据边界清单、核心实体关系分析、后续阶段技术预研。